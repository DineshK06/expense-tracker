server:
  port: 8080

jwt:
   secret: ${JWT_SECRET:A8VWfFdPvvb8vHl0WxyKW5VwHpiRNgxL5FPkGkhsklY=} # Secret key from environment
   expiration: ${JWT_EXPIRATION:36000000} # Default to 10 hours if not set

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/expense_tracker
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true


